import { StyleSheet, Text, View, Animated } from "react-native";
import { useRef } from "react";

export const GoalDashAnim = ({count, }) => {

 const dashAnim = useRef()

    


  return GoalDashAnim;
};
const styles = StyleSheet.create({});
